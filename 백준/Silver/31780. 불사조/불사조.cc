#include <iostream>
using namespace std;

int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    
    int X , M;
    cin >> X >> M;
    cout << X*(M+1)<<"\n";
    
    return 0;
}