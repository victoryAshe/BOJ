#include <iostream>
using namespace std;

int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    
    int N, K; cin >> N;
    for(int i = 0; i<N; i++)
    {
        cin >> K;
        cout << 1<<"\n";
    }
    
    return 0;
}